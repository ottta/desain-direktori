import HeaderProfile from "@/components/HeaderProfile";

export default function Loading() {
  return (
    <div>
      <HeaderProfile>Loading /submission/@profile...</HeaderProfile>
    </div>
  );
}
